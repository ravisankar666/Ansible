- name: install package
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop: 
    - nginx
    - mysql
    - zip